import Foundation

// Copy this file to `Secrets.swift` and fill in your Supabase project values.
// Values match desktop/.env (VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY).
enum Secrets {
    static let supabaseURL = URL(string: "https://your-project.supabase.co")!
    static let supabaseAnonKey = "your-anon-key"
}
